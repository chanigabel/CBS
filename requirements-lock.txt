# requirements-lock.txt
# -----------------------------------------------------------------------
# Locked runtime dependencies for Excel Data Standardization Web App
# Generated: 2026-04-30
# Python: 3.12.0 / Windows x64
# Updated: Security remediation — python-multipart, starlette, lxml, orjson
#          FastAPI updated to 0.120.1 (minimum required for starlette==0.49.1)
#
# IMPORTANT: This file is NOT a complete inventory of the already-delivered
# installer (ExcelNormalization_Setup_1.0.2.exe). That installer was built
# from a shared development environment and contains additional packages
# beyond this list (attrs, pyreadline3, websockets, setuptools, lxml, PyYAML)
# due to PyInstaller environment bleed. See docs/DEPENDENCY_INVENTORY.md,
# Section 5 for the full as-shipped inventory.
#
# This file contains ONLY the packages this project actually declares
# and requires at runtime (5 direct + 15 transitive).
# It does NOT include dev, test, or build tools.
#
# To produce a clean installer using this lock file:
#   python -m venv venv-release
#   venv-release\Scripts\activate
#   pip install -r requirements-lock.txt
#   pyinstaller ExcelNormalization.spec --noconfirm
# -----------------------------------------------------------------------

# --- Direct runtime dependencies ---
openpyxl==3.1.5
fastapi==0.120.1
uvicorn==0.35.0
python-multipart==0.0.26
Jinja2==3.1.6

# --- Transitive runtime dependencies ---
starlette==0.49.1
pydantic==2.11.9
pydantic_core==2.33.2
anyio==4.10.0
h11==0.16.0
click==8.2.1
MarkupSafe==3.0.2
et_xmlfile==2.0.0
sniffio==1.3.1
idna==3.10
typing_extensions==4.15.0
annotated-types==0.7.0
typing-inspection==0.4.1
colorama==0.4.6
annotated-doc==0.0.4
